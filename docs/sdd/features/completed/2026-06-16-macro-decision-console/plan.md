# Plan — Macro Decision Console

**Status**: Superseded
**Superseded by**: `docs/ARCHITECTURE.md`
**Date**: 2026-06-16
**Owning spec**: `docs/sdd/features/completed/2026-06-16-macro-decision-console/spec.md`
**Worktree**: `.worktrees/macro-decision-console`
**Branch**: `codex/macro-decision-console`
**Approved by**: Delegated goal from user on 2026-06-16
**Approved at**: 2026-06-16

## Pre-flight

- [ ] Create worktree: `git worktree add .worktrees/macro-decision-console -b codex/macro-decision-console main`.
- [ ] Verify worktree: `git worktree list`, `git status --short`, `git branch --show-current`.
- [ ] Run Parallax config diagnostic: `uv run parallax config`; record only redacted paths and booleans.
- [ ] Run macro status diagnostic: `uv run parallax macro status`; record counts, coverage, snapshot status, and missing concept labels.
- [ ] Run macrodata diagnostic in `/Users/qinghuan/Documents/code/macrodata-cli`: `uv run macrodata doctor`, `uv run macrodata bundle macro-core --asof 2026-06-16`.
- [ ] Baseline frontend gate: `cd web && npm run lint`, `cd web && npm run test:architecture`, `cd web && npm run typecheck`.
- [ ] Baseline Python macro gate: `uv run pytest tests/architecture/test_macro_no_compatibility_contract.py tests/unit/domains/macro_intel -q`.
- [ ] Baseline macrodata gate in `/Users/qinghuan/Documents/code/macrodata-cli`: `uv run pytest -q`, `uv run ruff check .`, `uv run mypy src tests`.

Known baseline findings from discovery:

- Parallax runtime uses `/Users/qinghuan/.parallax/config.yaml` and `/Users/qinghuan/.parallax/workers.yaml`; macrodata is enabled and FRED is configured through the `FINANCE_FRED_API_KEY` env name.
- `uv run parallax macro status` reports latest snapshot `partial`, history coverage `0.8425`, 20 concepts below minimum history, and projection lag `0`.
- Standalone macrodata-cli has `fred_api_key_configured=false`; `bundle macro-core` returned 67/128 available series and many FRED timeout errors through public CSV fallback.

## Timsun Alignment Audit — 2026-06-16

External benchmark read:

- `https://timsun.net/` is organized around a decision console first, then evidence pages. The first screen emphasizes macro state, confidence, key changes, confirmation/divergence, liquidity pressure, future catalysts, data quality, watch triggers, and trade map.
- Timsun keeps broader terminal surfaces than Parallax currently owns: equities/options/GEX/CFTC, bonds, commodities, FX, crypto, OKX+Deribit derivatives, Fed funds, yield curve, auctions, real rates, rate expectations, FOMC statements, speeches, liquidity transmission, global dollar, subsurface funding, VIX/volatility, CDS/credit stress, and daily market-event flow.
- Its trade map is not just labels; it has a five-asset radar (`NDX`, `BTC`, `GOLD`, `SPX`, `TLT`), deployed-capital/P&L framing, current action, rationale, risk temperature, historical trust, holding-period review, confirmation checklist, exit/risk events, and historical review rows.
- Its yield-curve page explains curve shape and bull/bear steepening or flattening, compares current/1w/1m/3m curves, shows spread history, compares nominal/real/breakeven tenors, and includes trade implication plus invalidation/time-frame language.

Current live Parallax read after this feature's runtime rebuild:

- Before Task 31, `uv run parallax macro status` reported macrodata-cli `0.1.9`, required bundle count `3`, missing bundle count `0`, `required_bundles_available=true`, 81,508 macro observations, 150 concepts, latest snapshot `ready`, `facts_max_observed_at=2026-06-16`, and projection lag `0`. After Task 32, Parallax was pinned to macrodata-cli `0.1.11` commit `ba8cf292afb77bfd554e0a0ebf1f3d0b0fc040fc`; after Task 36, Parallax was pinned to macrodata-cli `0.1.12` commit `25ba5281d04a0ddc81ab6a07c4a5784b698100f9`; after Task 37, Parallax was pinned to macrodata-cli `0.1.13` commit `1fde95d5b4ddff9bdec60cc9e1d25ec9027b10ce`; after Task 38, Parallax was pinned to macrodata-cli `0.1.14` commit `a90da8c3f4c7139924043d9d496493ded4326d50`; after the Task 29 GDPNow continuation, Parallax was pinned to macrodata-cli `0.1.15` commit `a01ed678ad578cd6406f93b20558da4ccd1fc660`; after the Task 26 NY Fed repo-depth continuation, Parallax was pinned to macrodata-cli `0.1.16` commit `06b94b1ccf5840ed34205498c4fddd43f796bb9d`; after the Task 30 NY Fed unsecured-funding continuation, Parallax was pinned to macrodata-cli `0.1.17` commit `ac06e171833a99e19761dc69a2e6a222d7f80754`; after the official Cboe VVIX/SKEW continuation, Parallax was pinned to macrodata-cli `0.1.18` commit `e02eef03feae897ddc9664f6d2bfead0ced8b2d9`; after the official Cboe VIX9D continuation, Parallax was pinned to macrodata-cli `0.1.19` commit `719634cccf32bb5d69aa3bc6a2d52b61b874f4f9`; after the official Cboe VIX1D continuation, Parallax was pinned to macrodata-cli `0.1.20` commit `739d0bab59f4ac8b905008478aeefbeb541e4a9b`; after the OKX/Deribit continuation, Parallax is pinned to macrodata-cli `0.1.22` commit `dd86aa8bcd234e8fb427ba9d058e9b478e2a0e6c`. The last unrestricted live status before the current sandbox change reported required bundle count `5`, missing required bundle count `0`, missing required bundle series count `0`, `required_bundle_series_available=true`, required series count `189`, concept count `174`, history readiness `true`, latest snapshot `ready`, and official Cboe VIX1D/VIX9D/VVIX/SKEW projected into `volatility/vix`.
- The 2026-06-17 OKX/Deribit continuation adds packaged macrodata-cli `0.1.22` provider/catalog/bundle/runtime/history support for `crypto-derivatives-core` and Parallax source support for importing/scheduling it. Parallax now expects five macro sync bundles, partitions all 14 OKX/Deribit series under `crypto-derivatives-core` rather than `macro-core`, and uses a portable Git dependency instead of a host-local checkout. In the current restricted shell, `macrodata bundle history crypto-derivatives-core` reaches the first-class command surface but provider requests return `ConnectError`, and `parallax macro sync` cannot reach the configured Postgres host; unrestricted live import/projection verification remains open.
- A 2026-06-17 read-only module audit against `/Users/qinghuan/.parallax/config.yaml` and `/Users/qinghuan/.parallax/workers.yaml` checked the current retained backend module ids after the hard-delete cleanups. The snapshot was `ready`, facts max observed date was `2026-06-16`, projection lag was `0`, and every retained backend module built through the same `build_macro_module_view(...)` path used by `/api/macro/modules/{module_id}`. Optional/reference concept gaps now degrade pages to `partial` instead of marking them `missing`: live `assets/commodities` and `volatility/vix` changed from misleading `缺失` headlines to `部分可用` because their stale/short-history gaps are optional references while required evidence and primary charts are present. Overview global gaps also use `global_reference` when the persisted macro snapshot is `ready`, so the first-screen decision console shows `partial` data-quality watch instead of a false missing-page state. After the redundant `liquidity/reserves`, generic `liquidity/transmission-chain`, generic `liquidity/operations`, and duplicate `liquidity/fed-balance-sheet` hard deletes, the live audit checks 16 retained backend module ids, reports no missing retained modules, and folds Fed assets, reserve balances, NY Fed RRP, and SRF evidence into `liquidity/rrp-tga`.
- All current numeric macrodata concepts are mapped to retained modules or retained overview catalysts; the latest repo-depth additions (`BGCR`, `TGCR`, and SOFR/BGCR/TGCR underlying volumes) are rendered inside `liquidity/rrp-tga`, while the latest unsecured-funding additions (`OBFR`, EFFR underlying volume, and OBFR underlying volume) are rendered inside `rates/fed-funds` through module observation series even while their long history bootstraps. Event concepts from `macro-calendar-core`, `treasury-auction-core`, and `fed-text-core` are retained as overview catalysts outside numeric scoring.
- The current overview scenario contains the intended decision-console keys: `confidence`, `current_regime`, `top_changes`, `confirmations`, `contradictions`, `trade_map`, `watch_triggers`, `invalidations`, `quality_blockers`, and `scenario_cases`.

Product conclusion:

- The first-screen decision-console and retained evidence pages are now usable on public data. The remaining distance to timsun is source depth and trade-map evidence quality, not route count.
- The first trade-map evidence-quality slice is implemented in this feature: overview `trade_map` now includes a source-backed five-asset 60-day historical review for `NDX`, `BTC`, `GOLD`, `SPX`, and `TLT`, with hit count, win-rate label, average return, max adverse excursion, and per-asset outcome rows. The Macro Workbench renders this evidence as structured `当前表达` / `五资产雷达` / `组合复盘` / `历史可信度` / `持有期复盘` / `行动清单` blocks instead of a flat text run. The overview scenario now also emits a timsun-style `scenario_cases` block with `基准情景` / `乐观情景` / `悲观情景`, probabilities, two-week thesis, trade expression, entry condition, stop, and invalidation. This uses existing Parallax macro observation histories and deterministic regime/trigger state; it does not add provider calls or frontend-side macro scoring.
- The overview `top_changes` lane now matches the TimSun-style `3 个最重要变化` block more closely. Feature-delta changes carry structured `change_label`, `value_label`, `source_label`, `observed_at`, `severity`, `severity_label`, and `evidence_label`, and the frontend renders that backend evidence directly under the `3 个最重要变化` title. This keeps the first-screen change list source-backed and machine-readable without React-side ranking or provider calls.
- The overview decision console now promotes Trade Map holding-period evidence into `judgement_review`, rendered as `昨日判断复盘` immediately after `交易映射`. Each row carries all available holding windows, currently 1D/5D/20D, plus the Trade Map historical trust summary; the old 1D-only top-level horizon/status/P&L shape is removed rather than kept as a compatibility path. This adds the TimSun-style review loop without fabricating a prior LLM judgement, creating a new table, calling providers from React, or adding compatibility routes.
- The overview decision console now emits a short-window `future_catalysts` block rendered as `未来 24/72h 催化剂` between `流动性压力` and `交易映射`. It merges explicit 24h/72h scenario watch triggers with source-backed official calendar and Treasury auction calendar events inside the next three days, while broader calendar rows, auction results, and Fed text documents move to the sibling `market_event_flow` stream instead of duplicated decision-console event lanes. This adds the TimSun-style near-term catalyst strip without restoring deleted calendar, auction, Fed text, or proxy routes, and without React-side event scoring.
- The overview decision console now replaces the old generic `观察触发 / 失效条件` pair with a backend-owned `watchlist_alerts` block rendered as `Watchlist 与触发提醒` after `未来 2 周情景` and before `数据可信度层`. Assets come directly from current Trade Map legs, while rules merge scenario watch triggers, invalidations, and quality blockers with explicit kind, window, and severity labels. This keeps the TimSun-style watchlist/action-trigger strip source-backed and deletes the redundant frontend `watchTriggers` / `invalidations` decision-console model fields instead of hiding the old section behind compatibility code.
- The overview module now emits a backend-owned `structured_analysis` block rendered as `跨域判断链` immediately after `今日决策台` and before the market board. It starts with a `市场主线` row derived from the current scenario, base-case thesis, top changes, Trade Map expression, and invalidation, adds source-backed `美联储沟通` when official Fed text catalysts are present, then compresses retained domain diagnostics for assets, rates, Fed policy, liquidity, growth, employment, inflation, volatility, and credit into regime, fact, evidence, trade implication, and invalidation rows. The row set is not hard-truncated, so fully populated snapshots keep every retained domain instead of dropping credit or volatility when the overview is most complete. The frontend maps and renders this payload only; it does not recompute cross-domain ranking, call providers, or keep a hidden compatibility section.
- The first asset-landing cross-asset decision-read slice is implemented in this feature: retained `assets` now emits backend `asset_diagnostics` with SPX, TLT, DXY, WTI, BTC, VIX, and HY OAS current/change rows, cross-asset regime language, trade implication, and invalidation. The asset landing page renders this directly after the core market board as `跨资产诊断`, so the page now reads like a source-backed market board instead of chart/table evidence plus generic support notes. The first retained asset-child slices are also implemented: `assets/equities` now emits backend `asset_class_diagnostics` with SPX, NDX, RUT, QQQ, IWM, and CFTC S&P net non-commercial positioning, rendered as `美股风险诊断`; `assets/bonds` now emits backend `asset_class_diagnostics` with TLT, IEF, LQD, HYG, HY OAS, and IG OAS, rendered as `债券风险诊断`; `assets/commodities` now emits backend `asset_class_diagnostics` with WTI, Brent, NatGas, Gold, and Copper, rendered as `商品冲击诊断`; `assets/fx` now emits backend `asset_class_diagnostics` with DXY, Broad USD, EURUSD, USDJPY, USDCNY, and UUP, rendered as `美元压力诊断`; `assets/crypto` now emits backend `asset_class_diagnostics` with BTC/ETH plus OKX/Deribit OI, funding, basis, and DVOL leverage diagnostics, rendered as `加密 beta 诊断`. Retained asset pages now also mark missing implemented asset-depth groups as warning-level `module_reference` gaps, including cross-asset breadth/duration/credit/volatility/commodity depth, equity leadership/small-cap/global-sector/positioning depth, bond duration/inflation-protection/credit/aggregate depth, commodity Brent/NatGas/precious-metals/copper/ETF depth, FX broad-dollar/G10/Asia/ETF depth, and crypto derivative groups. This uses existing Yahoo/CFTC/FRED/Cboe histories plus the new public OKX/Deribit bundle and does not restore a standalone crypto-derivatives route, options/GEX, standalone CFTC, CDS, commodity proxy shells, or any hidden asset compatibility route.
- The yield-curve decision-read slice is implemented in this feature: `rates/yield-curve` now emits backend `curve_diagnostics` with 2s10s, 3m10y, and 5s30s current/1w/1m/3m changes, bounded spread-history series, curve-shape language, trade implication, invalidation, and 5Y/10Y nominal-real-breakeven tenor comparison. It also marks missing implemented curve-depth groups for 3M front-end, 5Y belly, 30Y long-end, TIPS real-rate decomposition, and breakeven decomposition as warning-level `module_reference` gaps so a 2Y/10Y-only curve page is `partial` instead of silently complete. This uses existing FRED nominal Treasury, TIPS real-yield, and breakeven histories already present in the macro snapshot and does not restore any deleted rates route or add frontend-side curve scoring.
- The real-rates decision-read slice is implemented in this feature: `rates/real-rates` now emits backend `real_rate_diagnostics` with 5Y/10Y/30Y TIPS real-yield rows, 5Y/10Y breakeven plus 5Y5Y forward inflation rows, current/1w/1m/3m changes, real-rate-pressure regime language, trade implication, and invalidation. It also marks missing implemented real-rate-depth groups for full TIPS curve, breakeven curve, and 5Y5Y forward inflation as warning-level `module_reference` gaps so a 10Y-real-only page is `partial` instead of silently complete. This uses existing FRED TIPS and breakeven histories already present in the macro snapshot and does not add FedWatch/OIS proxies or frontend-side macro scoring.
- The first credit-stress decision-read slice is implemented in this feature: `credit/stress` now emits backend `credit_diagnostics` with HY OAS, IG OAS, CCC-HY tail spread, HYG/LQD credit ETF relative pressure, NFCI financial conditions, adjusted NFCI, and SLOOS large-firm tightening current/change rows, credit regime language, trade implication, and invalidation. It also marks missing implemented credit-depth groups for HYG/LQD ETF pressure, NFCI financial conditions, SLOOS bank lending, and loan quality as warning-level `module_reference` gaps so a spread-only credit page is `partial` instead of silently complete. This uses existing FRED/Yahoo credit histories already present in the macro snapshot and does not restore `credit/cds` or any hidden compatibility route.
- The first volatility decision-read slice is implemented in this feature: `volatility/vix` now emits backend `volatility_diagnostics` with VIX spot, VIX1D-VIX same-day event premium, VIX9D-VIX near-term event premium, VIX3M-VIX term premium, official Cboe VVIX convexity, official Cboe SKEW tail premium, MOVE rates volatility, VIXY/VIXM front-end pressure, and VXN current/change rows, volatility-regime language, trade implication, and invalidation. It also marks missing implemented depth groups for event premium, tail depth, rates vol, and futures-proxy pressure as warning-level `module_reference` gaps so a VIX-only page is `partial` instead of silently complete. This uses existing FRED/Cboe/Yahoo histories plus packaged `yahoo:^MOVE` and official Cboe index downloads; it does not restore the deleted volatility dashboard.
- The first liquidity RRP/TGA decision-read slice is implemented in this feature: `liquidity/rrp-tga` now emits backend `liquidity_diagnostics` with SOFR-IORB corridor pressure, SOFR-TGCR repo-depth pressure, SOFR underlying volume, RRP buffer, TGA fiscal cash, and net liquidity rows, liquidity-regime language, trade implication, and invalidation. It also marks missing implemented liquidity-depth groups for Fed assets/reserve balances, SOFR/IORB, BGCR/TGCR, SOFR/BGCR/TGCR volume, and NY Fed RRP/SRF operations as warning-level `module_reference` gaps so an RRP/TGA-only page is `partial` instead of silently complete. This uses existing NY Fed/FRED/Treasury histories plus newly packaged NY Fed BGCR/TGCR/underlying-volume series, and does not restore `liquidity/global-dollar` or `liquidity/subsurface`.
- The overview decision console now reuses retained `liquidity/rrp-tga` diagnostics as a first-screen `liquidity_pressure` block with score, regime, summary, top source-backed drivers, implication, and invalidation. This matches the TimSun-style homepage liquidity-pressure read while keeping detailed liquidity analysis on the retained RRP/TGA surface; it does not restore deleted liquidity category, transmission-chain, operations, reserves, global-dollar, or subsurface routes.
- The overview decision console now emits a source-backed `data_credibility` layer for retained core rows across SPX, DXY, BTC, WTI, 10Y, VIX, HY OAS, and ON RRP when enough rows exist. The block carries issue count, display value, unit, as-of, source, raw quality, and user-facing quality labels from the backend feature/tile projection; the Macro Workbench renders it as `数据可信度层` with existing quality blockers folded into the same section. This matches the TimSun-style first-screen data-trust table while avoiding React-side scoring, provider calls, hidden compatibility routes, or leaked provider series keys.
- The first Fed funds corridor decision-read slice is implemented in this feature: `rates/fed-funds` now emits backend `policy_diagnostics` with target range, EFFR position, EFFR-IORB, SOFR-EFFR, SOFR 30D-EFFR, DFF-EFFR, OBFR-EFFR, EFFR underlying volume, OBFR underlying volume, policy-corridor regime language, trade implication, and invalidation. It also marks missing implemented corridor-depth groups for DFF, SOFR 30D, OBFR, and EFFR/OBFR volume as warning-level `module_reference` gaps so a target/EFFR/IORB-only page is `partial` instead of silently complete. This uses existing Federal Reserve/NY Fed/FRED rate histories plus newly packaged NY Fed unsecured reference-rate and volume series, and does not add fake FedWatch probabilities or restore Fed text, rate-expectations, auction, or subsurface routes.
- The first Fed official text slice is implemented in this feature: macrodata-cli now exposes `fed-text-core` from official Federal Reserve FOMC calendar pages plus monetary-policy and speech RSS feeds; Parallax imports those observations as `event:*` catalysts with stable per-document series keys. The overview decision console now preserves official source URLs plus Fed `document_type` and speech `speaker` metadata so operators can inspect the primary document. This adds statements/minutes/press releases/speeches to the decision console without restoring the deleted Fed statements or speeches pages, and without adding hawk/dove text scoring yet.
- The first BLS official calendar slice is implemented in this feature: macrodata-cli now extends `macro-calendar-core` with official BLS CPI, Employment Situation, and PPI schedule pages. Parallax maps those observations to `event:*` overview catalysts outside numeric scoring, and the packaged runtime sync imports and projects the three BLS rows from the pinned macrodata-cli runtime. This adds next-release visibility, release time, reference period, and primary-source links without restoring a separate calendar/surprise page and without fabricating actual/consensus/prior/revision rows.
- The overview market-event-flow hard cut supersedes the earlier event heatmap slice: the overview no longer emits `decision_console.event_heatmap` or `decision_console.event_catalysts`. It emits sibling `module_read.market_event_flow` after `structured_analysis`, classifying official calendar, Treasury auction calendar/results, Fed text rows, and source-backed `news_page_rows` by window, severity, category, impact, watch text, market scope, asset tags, and source URL. BLS/BEA official calendar rows are labelled as `release_revision` / `实际/修正`, not surprise, because consensus expectations are not source-backed. This keeps future-event, auction-result, Fed-communication, and TimSun-style market-news visibility on retained `/macro` without restoring `rates/auctions`, Fed text pages, calendar/surprise routes, hidden sections, frontend news joins, or compatibility fields.
- The first inflation decision-read slice is implemented in this feature: `economy/inflation` now emits backend `inflation_diagnostics` with CPI, Core CPI, PPI, and 10Y breakeven current/change rows, inflation-regime language, trade implication, and invalidation. It also marks missing implemented inflation-depth groups for PCE/Core PCE, GDP deflator, market inflation expectations, and Michigan consumer expectations as warning-level `module_reference` gaps so a CPI/PPI-only page is `partial` instead of silently complete. This uses existing FRED inflation histories already present in the macro snapshot and does not add a proxy-only surprise/calendar page.
- The first employment decision-read slice is implemented in this feature: `economy/employment` now emits backend `employment_diagnostics` with unemployment-rate change, payroll monthly gain/deceleration, initial-claims change, job-openings change, wage YoY, labor-regime language, trade implication, and invalidation. It also marks missing implemented labor-depth groups for JOLTS openings, average hourly earnings, and labor participation as warning-level `module_reference` gaps so an unemployment/payroll/claims-only page is `partial` instead of silently complete. This uses existing FRED/BLS labor histories already present in the macro snapshot and does not add fake payroll surprise or consensus rows.
- The first GDP/growth decision-read slice is implemented in this feature: `economy/gdp` now emits backend `growth_diagnostics` with real GDP YoY/quarterly deceleration, source-backed Atlanta Fed GDPNow SAAR nowcast, industrial-production YoY, housing-starts level/change, real PCE YoY, retail-sales YoY, growth-regime language, trade implication, and invalidation. It also marks missing implemented growth-depth groups for nominal GDP, GDPNow, industrial production/housing, consumption, and consumer-buffer evidence as warning-level `module_reference` gaps so a real-GDP-only page is `partial` instead of silently complete. This uses existing FRED/BEA/BLS growth histories plus FRED `GDPNOW` already present in the macro snapshot and does not add fake release-surprise, consensus, prior, or revision rows.
- Deleted pages remain deleted. Do not restore auctions, rate expectations, Fed statements, Fed speeches, volatility dashboard, CDS, global-dollar, subsurface funding, consumer, crypto-derivatives, the duplicate bank-reserves liquidity route, the generic liquidity transmission route, or the generic public-operations liquidity route until a successor task implements the listed source, read model, product rendering, and verification gate. Bare category aliases such as `/macro/rates`, `/macro/liquidity`, `/macro/economy`, `/macro/volatility`, and `/macro/credit` are also deleted direct-entry surfaces; navigation links point to concrete retained modules and old category URLs use the ordinary route-error surface. `fed-text-core` is an overview catalyst feed, not a route-restoration gate by itself.
- Runtime module pages must not carry static future-source backlog labels. Source backlog lives in SDD/tech-debt docs until it becomes implemented data.

## Timsun Parity Successor Tasks

| Task | Scope | Source path | Restore gate |
|------|-------|-------------|--------------|
| P1 Trade-map reliability | Convert current explanatory trade map into five-asset radar plus deployed-capital/P&L, holding-period review, historical trust, MFE/MAE, action checklist, and a two-week scenario plan. Shipped slices: five-asset 60-day historical review with win-rate/outcome rows; `$10K` paper map with P&L, risk temperature, and action checklist; 1D/5D/20D holding-period review with historical trust summary; structured workbench rendering with separate current expression, radar, portfolio, trust, holding-period, and checklist blocks; `scenario_cases` for base/upside/downside with probability, trade, entry, stop, and invalidation. | Parallax's own daily macro snapshots, existing Yahoo/FRED asset histories, deterministic regime/trigger state, and deterministic backtest rules. | Remaining gate: recent historical maps table across prior macro snapshots. Current slices already prove NDX/BTC/GOLD/SPX/TLT history, paper P&L, historical trust, holding-period review, and two-week scenario planning without frontend provider calls or automated trade execution. |
| P1 Asset landing diagnostics | Convert retained `assets` from a grouped market table plus support rail into a timsun-style cross-asset read. Shipped slices: backend asset diagnostics for SPX, TLT, DXY, WTI, BTC, VIX, and HY OAS; child-page diagnostics for equities, bonds, commodities, FX, and crypto, including DXY/Broad USD/EURUSD/USDJPY/USDCNY/UUP dollar-pressure confirmation plus BTC/ETH beta confirmation and OKX/Deribit derivatives table evidence plus retained crypto leverage diagnostics; missing implemented asset-depth groups now surface as module-reference data-health gaps on retained asset pages. | Existing Yahoo/FRED/Cboe/CFTC macro histories in `macro-core`, current macro module snapshots, and new public OKX/Deribit `crypto-derivatives-core`; no request-time provider calls. | Remaining gate: options/GEX/breadth, ETF flows, and deeper positioning depth require source-backed successor lanes. Current slices cover core cross-asset, equity, bond, commodity, FX, spot crypto, first derivatives evidence/diagnostics, and honest retained-page source health without restoring `assets/crypto-derivatives`, options/GEX shells, standalone CFTC, or frontend scoring. |
| P1 Yield-curve diagnostics | Convert retained `rates/yield-curve` from chart/table evidence into timsun-style curve read. Shipped slices: backend curve diagnostics for 2s10s, 3m10y, and 5s30s with current/1w/1m/3m changes, bounded spread-history series, shape label, implication, invalidation, and 5Y/10Y nominal-real-breakeven tenor comparison rendered in the rates workbench; missing implemented 3M/5Y/30Y nominal curve and TIPS/breakeven decomposition groups now surface as module-reference data-health gaps. | Existing FRED nominal Treasury yield, TIPS real-yield, and breakeven histories in `macro-core` and current macro module snapshots. | Current slices cover curve shape, spread history, tenor decomposition, and honest retained-page source health without restoring `rates/auctions`, `rates/expectations`, or frontend scoring. Remaining rates gates are FedWatch/meeting probabilities, Fed text deltas, OIS, and auction-tail/liquidity-impact sources. |
| P1 Credit-stress diagnostics | Convert retained `credit/stress` from chart/table evidence into timsun-style credit read. Shipped slice: backend credit diagnostics for HY OAS, IG OAS, CCC-HY tail, HYG/LQD relative credit ETF pressure, NFCI/ANFCI financial conditions, and SLOOS large-firm tightening with current/change rows, regime label, implication, and invalidation rendered in the leaf module workbench; missing implemented ETF, NFCI, SLOOS, and loan-quality groups now surface as module-reference data-health gaps. | Existing FRED OAS, Chicago Fed NFCI/ANFCI, SLOOS, FRED loan-quality series, and Yahoo HYG/LQD histories in `macro-core` and current macro module snapshots. | Remaining gate: TRACE liquidity, ETF premium/discount/flows, and licensed CDS if approved. Current slice already covers public credit-stress, ETF confirmation/contradiction, financial-conditions decision language, and honest retained-page source health without restoring `credit/cds` or frontend scoring. |
| P1 Fed text lane | Shipped slice: official FOMC statement/minutes, monetary-policy press-release, and speech ingestion into `fed-text-core`, then Parallax overview catalysts with source links, document type, and speech speaker metadata. Remaining slice: hawk/dove deltas, statement diffing, richer speaker context, and policy narrative read model. | Federal Reserve FOMC calendars, statements/minutes pages, monetary-policy RSS, speeches RSS. | Keep deleted Fed text routes deleted until a source-backed text read model explains deltas and speaker context; the current bundle is catalyst-only plus primary-source inspectability. |
| P1 Rate-probability lane | Replace the deleted short-rate proxy expectations page with meeting-date probabilities and historical changes. | CME FedWatch API/DataMine or another approved legal source; public CME pages are research references, not assumed automation inputs. | `rates/expectations` stays deleted until a successor implements source-backed meeting probabilities, 1d/1w/1m change, and source licensing state; no scraped workaround or proxy page. |
| P1 Fed funds corridor diagnostics | Convert retained `rates/fed-funds` from chart/table evidence into timsun-style policy-corridor read. Shipped slices: backend policy diagnostics for target range, EFFR position, EFFR-IORB, SOFR-EFFR, SOFR 30D-EFFR, DFF-EFFR, OBFR-EFFR, EFFR volume, and OBFR volume with current/change rows, regime label, implication, and invalidation rendered in the rates workbench; NY Fed EFFR is folded into the existing `fed:effr` concept with higher priority than the FRED mirror; missing implemented DFF, SOFR 30D, OBFR, and volume groups now surface as module-reference data-health gaps. | Existing Federal Reserve, NY Fed, and FRED policy-rate histories in `macro-core`; newly packaged NY Fed Markets API unsecured EFFR/OBFR rate and volume series; current macro module snapshots. | Remaining gate: FedWatch/meeting probabilities and Fed statement/speech text deltas require approved source lanes. Current slice covers policy corridor, secured/unsecured overnight-funding pressure, funding depth, and honest retained-page source health without restoring Fed text, auction, rate-expectations, or subsurface routes. |
| P1 Volatility term structure | Convert retained `volatility/vix` from chart/table evidence into timsun-style volatility read. Shipped slice: backend volatility diagnostics for VIX spot, VIX1D-VIX, VIX9D-VIX, VIX3M-VIX, official Cboe VVIX, official Cboe SKEW, MOVE, VIXY/VIXM, and VXN with current/1w/1m changes, regime label, implication, and invalidation rendered in the leaf module workbench. | Existing FRED/Cboe/Yahoo VIX, VIX3M, VXN, VIXY, VIXM histories, official Cboe VIX1D/VIX9D/VVIX/SKEW historical CSV downloads, plus macrodata `yahoo:^MOVE` in `macro-core` and current macro module snapshots. | Remaining gate: CFE VIX futures curve, VXST/VXV/VXMT, realized vol, volume/OI, longer tenor term-structure evidence, and licensed ICE/Bloomberg MOVE or intraday redistribution where source-backed. Current slice already covers spot/same-day and near-term event premium/term proxy/front-end pressure/rates-vol/convexity/tail-premium decision language without restoring the volatility dashboard. |
| P1 Liquidity RRP/TGA diagnostics | Convert retained `liquidity/rrp-tga` from chart/table evidence into timsun-style liquidity read. Shipped slices: backend liquidity diagnostics for SOFR-IORB, SOFR-TGCR, SOFR volume, RRP buffer, TGA, and net liquidity with current/1w/1m changes where history exists, regime label, implication, and invalidation rendered in the leaf module workbench; overview 14-day official event heatmap for Fed/BEA/BLS calendar rows and Treasury auction calendar rows; module observation-series supplementation so optional repo-depth concepts display as soon as facts are projected even before they enter global regime snapshot features; missing implemented balance-sheet, secured-corridor, repo-depth, repo-volume, and NY Fed operations groups now surface as module-reference data-health gaps. | Existing NY Fed/FRED/Treasury SOFR, IORB, ON RRP, TGA, and Fed assets histories in `macro-core`; newly packaged NY Fed Markets API BGCR/TGCR and SOFR/BGCR/TGCR underlying volumes; official event bundles in `macro-calendar-core`, `treasury-auction-core`, and `fed-text-core`; current macro module snapshots. | Remaining gate: 7d/14d projected net issuance/liquidity impact, auction tail from a reliable when-issued source, OFR STFM unsecured funding depth/percentiles, and cross-currency/global dollar evidence. Current slices cover corridor, repo-depth first layer, balance-sheet pressure, official-operations source health, and near official event timing without restoring global-dollar, subsurface, auctions, or calendar pages. |
| P1 Crypto derivatives | Shipped packaged slice: macrodata-cli `crypto-derivatives-core` with OKX BTC/ETH perp OI, funding, basis plus Deribit BTC/ETH perp OI, 8h funding, basis, and DVOL. Parallax imports/maps/schedules the bundle and folds evidence into retained `assets/crypto` tables plus crypto leverage diagnostics for OI expansion/flush, hot/negative funding, rich/backwardated basis, and DVOL stress. | OKX public REST APIs and Deribit public market-data API; paid historical normalization via Amberdata/Kaiko/Tardis only if approved. | Remaining gate: unrestricted live sync/projection, stale-source checks, normalized history, and richer term/expiry views before any standalone `assets/crypto-derivatives` route can be rebuilt. The package/pin and retained diagnostic gates are complete at macrodata-cli `0.1.22` / `dd86aa8bcd234e8fb427ba9d058e9b478e2a0e6c`. |
| P2 Options/GEX/breadth | Add US option open-interest, volume, breadth, ETF flow, and dealer-gamma source decisions. | OCC open interest/series search, Cboe daily stats, issuer ETF flow/premium pages; OPRA/Cboe DataShop/SpotGamma if licensed. | Asset pages can expose options/GEX confirmation only after strike/expiry OI and calculation assumptions are tested. |
| P2 Global-dollar funding | Add cross-currency basis, offshore dollar, and dollar funding stress proxies. | BIS SDMX for lower-frequency official statistics; Bloomberg/Refinitiv for daily cross-currency basis if licensed. | Do not restore `liquidity/global-dollar` until basis/funding concepts are present and freshness is explicit. |
| P2 Subsurface funding | Deepen SOFR/IORB corridor with remaining percentiles, term repo stress, OFR/STFM distribution detail, and global-dollar funding links. First NY Fed repo-depth slice is already folded into `liquidity/rrp-tga` via BGCR/TGCR and SOFR/BGCR/TGCR underlying volumes; first NY Fed unsecured slice is folded into `rates/fed-funds` via OBFR/EFFR and underlying volumes. The retained RRP/TGA page now surfaces missing implemented balance-sheet, secured-corridor, repo-depth, repo-volume, and NY Fed operations groups as module-reference data-health gaps; OFR/STFM distributions and cross-currency basis remain restore gates. | NY Fed Markets APIs/reference rates, OFR STFM, H.4.1/FRED, BIS/Bloomberg/Refinitiv if licensed for cross-currency basis. | Do not restore `liquidity/subsurface` until intraday/daily funding pressure is source-backed beyond the retained RRP/TGA and Fed funds decision pages. |
| P2 Credit microstructure | Add TRACE aggregate liquidity, ETF premium/discount, and CDS if licensed. | FINRA TRACE Market Reports/API and ETF issuer pages; Markit/Bloomberg for CDS if approved. | Do not restore `credit/cds`; keep CDS as paid-source gap unless a legal feed exists. |
| P2 Economy nowcast/surprise | Convert retained economy pages into timsun-style data reads. Shipped slices: backend GDP/growth diagnostics for real GDP, GDPNow, industrial production, housing starts, real PCE, and retail sales; backend inflation diagnostics for CPI, Core CPI, PPI, and 10Y breakeven; backend employment diagnostics for unemployment, payroll gain, initial claims, JOLTS openings, and wage YoY with regime label, implication, and invalidation rendered in the leaf module workbench; official BLS/BEA next-release catalysts in overview market-event flow labelled as release/revision watch rather than surprise; missing implemented economy-depth groups now surface as module-reference data-health gaps on the retained economy pages. | Existing FRED/BEA/BLS GDP, growth, GDPNow, CPI/Core CPI/PPI/breakeven, and labor histories in `macro-core`; official BLS/BEA schedule pages in `macro-calendar-core`; current macro module snapshots. | Remaining gate: BLS/BEA/FRED release actual-vs-consensus, prior, revisions, and surprise history. Economy pages should show actual/consensus/prior/revision and source-backed surprise contribution before adding a separate calendar/surprise page. |

## File-Level Edits

### `src/parallax/domains/macro_intel/services/macro_module_catalog.py`

- Lines 41-72: replace the broad 31-id `MACRO_MODULE_IDS` inventory with a retained allowlist only. Do not add a hidden/deferred tier.
- Lines 75-726: delete module configs for auctions, rate expectations, Fed statements, Fed speeches, volatility dashboard, CDS proxy, global dollar, subsurface funding, consumer, crypto derivatives, duplicate bank reserves, generic liquidity transmission, and generic public operations unless the implementation finds one is genuinely source-backed and decision-useful.
- Lines 88-95, 157-165, 281-282, 380-403, 536-555, 659-724: remove deleted routes from every `related_routes` list.
- Add tests in `tests/unit/domains/macro_intel/test_macro_module_catalog.py` asserting retained ids, deleted ids absent from the catalog, and no retained config links to deleted routes.

### `src/parallax/domains/macro_intel/services/macro_scenario_engine.py`

- Lines 1-220: add a deterministic decision-console shaper that uses existing `chain`, `panels`, `features`, `triggers`, and `data_gaps` to emit `top_changes`, `confirmations`, `contradictions`, `watch_triggers`, `invalidations`, `trade_map`, `quality_blockers`, and `scenario_cases`.
- Keep only one current contract for new data. Do not add duplicate legacy field names.
- Add unit tests that fixture a tightening/funding-stress scenario and assert output has no empty top-change labels, no raw gap codes, and deterministic ordering.

### `src/parallax/domains/macro_intel/services/macro_regime_engine.py`

- Lines 1-120: thread the decision-console fields into `scenario_json` or a new nested `decision_console` section inside `scenario_json`.
- Lines 121-180: ensure `scorecard_json` keeps coverage and gap counts needed by the console.
- Add a unit test for `build_macro_view_snapshot(...)` asserting `scenario_json.decision_console` exists for partial snapshots and includes data-health blockers.

### `src/parallax/domains/macro_intel/services/macro_module_views.py`

- Lines 1-220: delete proxy-only view construction for removed module ids. The view builder should only know retained module ids.
- Ensure removed ids reach the same not-found path as unknown ids; do not add a special unavailable/deferred module view.
- Add tests asserting `rates/auctions`, `fed/statements`, `volatility/dashboard`, and `credit/cds` are not recognized module views.

### `src/parallax/app/surfaces/api/routes_macro.py`

- Locate the module route builder and ensure deleted module ids return ordinary not-found behavior, matching unknown ids.
- Ensure `/api/macro` includes the decision-console block from the persisted snapshot.
- Add API tests for `/api/macro` decision-console payload and deleted module ids returning not found.

### `web/src/features/macro/model/macroNavigationTree.ts`

- Lines 19-330: reduce public navigation to primary source-backed routes:
  - Keep: overview, assets, assets/equities, assets/bonds, assets/commodities, assets/fx, assets/crypto, rates/fed-funds, rates/yield-curve, rates/real-rates, liquidity/rrp-tga, economy/gdp, economy/employment, economy/inflation, volatility/vix, credit/stress.
  - Delete: assets/crypto-derivatives, rates/auctions, rates/expectations, fed/statements, fed/speeches, liquidity/global-dollar, liquidity/reserves, liquidity/transmission-chain, liquidity/operations, liquidity/subsurface, economy/consumer, volatility/dashboard, credit/cds.
- Retain `/api/macro/assets/correlation` only as the asset landing page's source-backed support data; hard-delete `/macro/assets/correlation` as a standalone navigable page, route descriptor, breadcrumb target, sidebar leaf, and responsive-audit product route.
- Remove hidden-label machinery from `web/src/features/macro/model/macroPageRegistry.ts` when it only exists to preserve deleted pages.
- Update architecture tests that assert route descriptors and page registry behavior.

### `web/src/features/macro/model/macroRoutes.ts`

- Delete parent-category redirect aliases for bare paths such as `/macro/rates` and `/macro/liquidity`. They are direct-entry compatibility surfaces, not retained product pages.
- Keep `/macro` and concrete retained module URLs as the only module routes; unknown/deleted macro tails return `null` and use the ordinary route-error surface.

### `web/src/features/macro/ui/pages/MacroOverviewModulePage.tsx`

- Replace the current overview composition with a decision-console reading order:
  1. Macro State strip: regime, confidence, as-of, coverage.
  2. Top Changes: three highest-impact source-backed changes.
  3. Confirm / Diverge: confirmations and contradictions.
  4. Trade Map: expression, time window, invalidations.
  5. Watch Triggers: next 24/72h or deterministic watch triggers.
  6. Future 2-week scenarios: base/upside/downside probability, thesis, trade, entry, stop, invalidation.
  7. 14-day event heatmap: upcoming official macro and Treasury supply events with severity, window, impact, watch text, and source URL.
  8. Data Quality Watch: blockers and source health.
- Keep existing `MacroMarketBoard`, `MacroInsightBrief`, and `MacroDiagnosticsPanel` primitives when they fit; do not create a new global CSS bucket.
- Add component tests to assert the order above and absence of raw gap codes.

### `web/src/features/macro/ui/pages/MacroModulePageRenderer.tsx`

- Remove any route branches that render deleted module ids.
- Ensure deleted routes are not reachable through the macro page registry, route fixtures, or module renderer.

### `web/src/features/macro/ui/pages/macroPages.css` and local macro CSS files

- Keep styles in `web/src/features/macro/ui/pages/macroPages.css` or adjacent owner CSS.
- Add compact console layout classes only under the macro namespace.
- Verify no retired global CSS files or shared UI internals are restyled.

### External macrodata-cli files

- In `/Users/qinghuan/Documents/code/macrodata-cli/src/macrodata/providers/fred.py`: keep API-key mode as primary, add redacted source-mode metadata, and distinguish public CSV fallback timeout from API failure.
- In `/Users/qinghuan/Documents/code/macrodata-cli/src/macrodata/app/services.py`: split macro-core diagnostics by provider/source mode while preserving the macro-core bundle name.
- In `/Users/qinghuan/Documents/code/macrodata-cli/src/macrodata/core/models.py`: make a single current diagnostics contract if typed support is needed; do not keep parallel legacy diagnostic fields.
- In `/Users/qinghuan/Documents/code/macrodata-cli/README.md` and reference docs: document provider diagnostics and required FRED API-key behavior.

### Documentation

- Update `docs/ARCHITECTURE.md` and `src/parallax/domains/macro_intel/ARCHITECTURE.md` if API/read-model semantics change.
- Update `docs/FRONTEND.md` only if route deletion/frontend ownership rules change.
- Append source backlog to `docs/TECH_DEBT.md` only for items not implemented in this feature.

## Data-Source Gap Backlog

| Domain | Gap | Public candidate | Paid candidate | Priority | Product impact |
|--------|-----|------------------|----------------|----------|----------------|
| Trade Map | Historical reliability, P&L framing, five-asset radar, MFE/MAE, and holding-period review | Parallax daily snapshots plus Yahoo/FRED histories | Not required initially | P1 | Upgrades `/macro` from explanatory trade labels to timsun-style current + historical decision audit. |
| Rates | Treasury auction calendar/results, bid-to-cover, tail, indirect/direct demand | TreasuryDirect auction XML/CSV, FiscalData where available | Bloomberg/Refinitiv | P1 | Auction calendar/results now render as overview catalysts, and near upcoming auction calendar rows are included in the 14-day event heatmap. Rebuild `rates/auctions` only after auction-tail and liquidity-impact sources exist. |
| Rates | Fed funds futures / meeting probabilities | CME public pages may not be stable for automated use; evaluate legal/technical path | CME DataMine, Bloomberg WIRP | P1 | Rebuilds `rates/expectations` only after legal source-backed meeting probabilities replace the deleted short-rate proxy page. |
| Fed | Hawk/dove text deltas, statement diffing, richer speaker context, and Fed narrative scoring | Official Federal Reserve statements/minutes/press releases/speeches now arrive through `fed-text-core`; catalyst rows preserve source URL, document type, and speech speaker when available; additional local NLP/read-model work is needed | Bloomberg ECO/Fedspeak | P1 | Current official text documents render as inspectable overview catalysts; rebuild Fed text pages only after tested delta/scoring output exists. |
| Liquidity | Cross-currency basis and global dollar funding | BIS/FRB/H.4.1 where public, FRED limited proxies | Bloomberg/Refinitiv | P2 | Rebuilds global dollar page. |
| Liquidity | Intraday repo pressure and volumes | NY Fed SOFR percentiles/volumes if endpoint supports it | DTCC, Bloomberg | P2 | Rebuilds subsurface page. |
| Volatility | CFE VIX futures curve, VXST/VXV/VXMT, realized vol, option volume/OI, and longer tenor term structure | Cboe public downloads/pages where license permits; VIX1D/VIX9D/VVIX/SKEW are now implemented from official Cboe CSV downloads | Cboe DataShop, Bloomberg | P1 | Keeps the deleted vol dashboard out until true futures-curve and options-surface evidence exists; retained `volatility/vix` now carries VIX1D same-day premium, VIX9D near-term premium, VVIX/SKEW tail-risk depth, and module-reference gaps when those implemented depth groups are absent. |
| Volatility | Licensed MOVE / intraday rates-vol feed | Public `yahoo:^MOVE` is implemented as a daily proxy; official ICE/BofA redistribution and intraday depth still need an approved licensed path | ICE/Bloomberg | P2 | Improves rates-vol confirmation beyond the Yahoo proxy. |
| Credit | ETF premium/discount and TRACE liquidity | iShares/SPDR public ETF pages, FINRA TRACE aggregate files | Bloomberg/TRACE licensed | P2 | Adds market-liquidity confirmation. |
| Assets | US options breadth, GEX, dealer positioning, and ETF flows | OCC open interest/series search, Cboe daily market statistics, ETF issuer flows | OPRA, Cboe DataShop, SpotGamma, Bloomberg | P2 | Improves asset confirmation; do not add options/GEX UI until strike/expiry OI and assumptions are source-backed. |
| Assets | Crypto-derivatives depth and normalized history | OKX/Deribit first public slice is packaged in macrodata-cli `0.1.22`, pinned in Parallax, and consumed by retained `assets/crypto` leverage diagnostics; remaining public work is unrestricted live sync/projection, stale-source checks, richer term structure/expiry views, and normalized history | Amberdata, Kaiko, Tardis | P1 | Keep `assets/crypto-derivatives` deleted until derivatives evidence has live projected history, stale-source health, and a distinct source-backed decision surface beyond the retained crypto page. |
| Economy | Parallax release-calendar ingestion and surprise history | macrodata `macro-calendar-core`, BLS/BEA/FRED release tables, actual/prior/revision fields where public and legally automatable | Bloomberg ECO | P1 | GDPNow is now implemented as a source-backed retained GDP-page row; remaining work is actual/consensus/prior/revision/surprise history before any separate calendar/surprise page can be rebuilt. |

Implemented public-source additions in this feature:

| Domain | Source | Series | Product impact |
|--------|--------|--------|----------------|
| Assets | Yahoo/FRED public equity proxies and CFTC positioning | `SP500`, `NASDAQCOM`, `^GSPC`, `^NDX`, `^DJI`, `^RUT`, `SPY`, `QQQ`, `DIA`, `IWM`, `EFA`, `EEM`, `SMH`, `SOXX`, CFTC S&P 500 positioning | Folds public index, ETF, sector, global equity, and positioning evidence into `assets/equities`. |
| Assets | Yahoo public bond/commodity/FX proxies and FRED spot series | `SHY`, `IEF`, `TIP`, `BND`, `JNK`, `SLV`, `UNG`, `CPER`, FRED WTI/Brent/natgas, Yahoo commodity futures, Yahoo/FRED FX, `UUP`, `FXE`, `FXY` | Folds duration, credit, commodity, and currency proxies into retained asset pages instead of adding duplicate pages. |
| Assets | OKX and Deribit public crypto derivatives | `crypto-derivatives-core`: OKX BTC/ETH perp OI, funding, basis; Deribit BTC/ETH perp OI, 8h funding, basis, and BTC/ETH volatility indexes | Adds source-backed crypto derivatives evidence and retained crypto leverage diagnostics to `assets/crypto` without restoring the deleted `assets/crypto-derivatives` route; Parallax is pinned to the packaged macrodata-cli `0.1.22` bundle and still needs unrestricted live sync/projection verification. |
| Rates | FRED daily effective fed funds | `DFF` | Adds daily effective fed funds evidence to `rates/fed-funds` alongside EFFR, IORB, SOFR, and target bounds. |
| Rates | FRED TIPS and breakeven curve | `DFII5`, `DFII10`, `DFII30`, `T5YIE`, `T10YIE`, `T5YIFR` | Expands `rates/real-rates` from a 10Y-only view into a source-backed real-yield and inflation-compensation diagnostic page. |
| Economy | FRED growth breadth | `GDP`, `INDPRO`, `HOUST`, `CIVPART` | Folds nominal GDP, industrial production, housing starts, and labor participation into retained growth/employment pages. |
| Economy | FRED / Atlanta Fed GDPNow | `GDPNOW` | Adds a source-backed nowcast row to `economy/gdp` growth diagnostics without fabricating consensus, surprise, prior, or revision fields. |
| Economy | FRED inflation breadth and expectations | `GDPDEF`, `PCEPI`, `PCEPILFE`, `T5YIE`, `T10YIE`, `T5YIFR`, `MICH` | Expands `economy/inflation` beyond CPI/PPI into PCE, GDP deflator, market compensation, and survey expectations. |
| Economy | FRED consumer spending and sentiment | `PCE`, `PCEC96`, `PSAVERT`, `UMCSENT` | Folds consumer evidence into `economy/gdp` after deleting `economy/consumer`; removes the stale personal-spending gap label. |
| Economy | FRED/BLS labor demand and wages | `JTSJOL`, `CES0500000003` | Adds job openings and average hourly earnings to `economy/employment`; removes stale JOLTS/hourly-earnings gap codes. |
| Volatility | FRED/Cboe cross-asset volatility indexes | `VXNCLS`, `RVXCLS`, `GVZCLS`, `OVXCLS`, `EVZCLS` | Adds Nasdaq, Russell, gold, crude oil, and euro volatility evidence to `volatility/vix` without restoring the deleted volatility dashboard. |
| Volatility | Yahoo Finance / ProShares VIXM | `VIXM` | Adds a mid-term VIX futures ETF proxy to `volatility/vix` next to VIXY while keeping the true VIX futures curve gap visible. |
| Volatility | Official Cboe index history downloads | `VIX1D`, `VIX9D`, `VVIX`, `SKEW` | Adds same-day/near-term S&P 500 event-volatility premium, VIX volatility-of-volatility convexity, and S&P 500 tail-risk premium evidence to retained `volatility/vix`; no deleted volatility dashboard or static source-backlog row is restored. |
| Credit | FRED rating ladder and financial-condition indexes | `BAMLC0A1CAAA`, `BAMLC0A2CAA`, `BAMLC0A3CA`, `BAMLC0A4CBBB`, `BAMLH0A1HYBB`, `BAMLH0A2HYB`, `STLFSI4`, `NFCI`, `ANFCI` | Adds credit-quality ladder and broad stress indexes to `credit/stress` without reopening CDS/dealer pages. |
| Credit | FRED SLOOS | `DRTSCILM`, `DRTSCIS`, `DRSDCILM`, `DRSDCIS` | Adds bank credit supply and demand evidence to `credit/stress` without reopening a proxy-only page. |
| Credit | FRED loan quality | `DRBLACBS`, `DRCLACBS`, `CORBLACBS`, `CORCACBS` | Adds delinquency and charge-off evidence to `credit/stress` for late-cycle credit damage. |
| Calendar | Federal Reserve, BEA, and BLS official public calendars | `official_calendar:fomc_decision_next`, `official_calendar:bea_gdp_next`, `official_calendar:bea_pce_next`, `official_calendar:bls_cpi_next`, `official_calendar:bls_employment_next`, and `official_calendar:bls_ppi_next` in macrodata `macro-calendar-core` | Adds a clean future-catalyst data bundle for reachable Fed/BEA/BLS events and imports it into Parallax as `event:*` decision-console catalysts without polluting numeric `macro-core` regime history; `macrodata bundle history macro-calendar-core` and Parallax default `macro_sync.bundle_names` make it schedulable without restoring deleted calendar/Fed proxy pages. |
| Treasury Auctions | U.S. Treasury tentative schedule XML plus FiscalData completed auction results | `treasury_auction:*_next_auction_days` 2Y/10Y/30Y calendar events plus 2Y/10Y/30Y high yield, bid-to-cover, and indirect bidder accepted percentage in macrodata `treasury-auction-core` | Adds source-backed upcoming auction calendar and completed result catalysts to Parallax overview via `event:*` observations without restoring the deleted `rates/auctions` route; `macrodata bundle history treasury-auction-core` and Parallax default `macro_sync.bundle_names` make them schedulable, while auction tail remains a source gap until a reliable when-issued yield source exists. |
| Federal Reserve Text | Official Federal Reserve FOMC calendar pages and RSS feeds | `official_fed_text:fomc_statement_latest`, `official_fed_text:fomc_minutes_latest`, `official_fed_text:monetary_policy_press_release_latest`, `official_fed_text:speech_latest` in macrodata `fed-text-core` | Adds source-backed Fed text catalysts to Parallax overview via `event:*` observations with primary-source links and Fed document metadata, without restoring deleted Fed routes; `macrodata bundle history fed-text-core` and Parallax default `macro_sync.bundle_names` make document catalysts schedulable, while hawk/dove delta scoring remains a source/read-model gap. |

## PR Breakdown

1. **PR 1 — Macro hard deletion and route allowlist**: module catalog cleanup, frontend navigation cleanup, deleted-route not-found tests.
2. **PR 2 — Decision console read model and overview UI**: scenario decision fields, `/api/macro` payload, overview layout, component tests.
3. **PR 3 — macrodata diagnostics, bundle resilience, and official events**: FRED source-mode diagnostics, bundle coverage summaries, `macro-calendar-core`, `treasury-auction-core`, `fed-text-core`, event-aware Parallax importer/projection handling, default multi-bundle `macro_sync.bundle_names`, macrodata tests and docs.
4. **PR 4 — Source backlog docs and operator verification**: architecture/doc updates, live macro status verification, SDD verification.

## Analyze Gate

| Check | Result |
|-------|--------|
| Spec goals map to file-level edits. | Pass: G1 maps to scenario/API/overview edits; G2 maps to catalog/API/frontend hard deletion; G3 maps to macrodata FRED/bundle edits; G4 maps to source backlog docs. |
| Plan preserves canonical architecture boundaries. | Pass: provider IO stays in macrodata/macro_sync; API and frontend read persisted views; no UI scoring is introduced. |
| Compatibility code or old files are not retained. | Pass: weak macro routes are deleted rather than hidden, deferred, or direct-link supported. |
| Parallel touch/conflict sets are explicit. | Pass: tasks split macrodata provider, Parallax domain/API, frontend route/UI, and docs with named conflict sets. |

## Rollout Order

1. Merge hard route deletion so the product stops advertising weak pages.
2. Merge decision-console payload and `/macro` overview UI.
3. Merge macrodata FRED/bundle diagnostics and standalone official event bundles.
4. Pin a macrodata-cli Git rev that exposes event bundle history commands; `macro status` should show `required_bundles_available=true`.
5. Run live `uv run parallax macro sync --bundle macro-core --start <date> --end <date>` only if operator explicitly wants a runtime refresh.
6. Verify `/macro` and retained primary child routes in desktop and mobile browser sessions.

## Rollback

1. Revert PR 1 to restore the previous route inventory. No compatibility branch remains in the shipped code.
2. Revert scenario/API decision-console fields.
3. Revert frontend overview layout changes.
4. Revert macrodata diagnostics changes and any matching Parallax importer update.
5. No destructive data migration is planned. If a migration becomes necessary, add a separate rollback section before implementation.

## Acceptance test commands

- AC1: `cd web && npm run test -- web/tests/component/features/macro/MacroModulePages.test.tsx -t "renders overview decision console" --run`
- AC2: `cd web && npm run test:architecture`
- AC3: `cd web && npm run test -- web/tests/routes/macro.route.test.tsx -t "removed macro routes are not registered" --run`
- AC4: `cd /Users/qinghuan/Documents/code/macrodata-cli && uv run pytest tests/provider/test_fred_provider.py tests/unit/test_bundles.py -q`
- AC5: `uv run parallax macro status`
- AC6: `make check-all`
- AC7: `uv run pytest tests/unit/domains/macro_intel/test_macrodata_bundle_importer.py::test_import_macrodata_bundle_accepts_event_bundles_without_expanding_numeric_macro_core tests/unit/domains/macro_intel/test_macro_view_projection_worker.py::test_macro_view_projection_worker_event_targets_refresh_without_numeric_snapshot tests/unit/domains/macro_intel/test_macro_module_views.py::test_overview_module_view_adds_official_events_to_market_event_flow tests/unit/test_api_macro_contract.py::test_macro_overview_module_api_loads_event_concepts_for_market_event_flow -q && cd web && npm run test -- tests/unit/features/macro/model/macroWorkbenchModel.test.ts tests/component/features/macro/MacroModulePages.test.tsx --run`
- AC8: `uv run pytest tests/unit/domains/macro_intel/test_macro_sync_service.py::test_sync_service_enqueue_due_windows_schedules_all_configured_product_bundles tests/unit/test_worker_settings.py::test_default_workers_yaml_contains_canonical_worker_defaults tests/unit/test_cli_macro_commands.py::test_macrodata_runtime_state_reports_missing_configured_sync_bundles tests/architecture/test_kiss_runtime_invariants.py::test_macro_sync_worker_and_service_use_formal_settings_wake_contract_without_runtime_defaults -q && cd /Users/qinghuan/Documents/code/macrodata-cli && uv run pytest tests/cli/test_bundle_commands.py::test_event_bundle_history_commands_are_first_class_sync_surfaces tests/unit/test_bundles.py::test_bundle_history_marks_empty_series_windows_unavailable -q`

## Verification

Verification evidence lives in `docs/sdd/features/completed/2026-06-16-macro-decision-console/verification.md`. The verification artifact must be filled before the feature directory moves to `completed/`.
